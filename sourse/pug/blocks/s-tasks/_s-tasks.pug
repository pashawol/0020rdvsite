mixin s-tasks()
	// start s-tasks
	+b.SECTION.s-tasks.section.block-with-lazy#s-tasks&attributes(attributes)
		.container
			+b.section-title 
				h2 Другие задачи автоматизации
				
			ul
				- for (var x = 0; x < 8; x++)
					li
						a(href="#") Настройки выгрузки товаров, свойств, картинок в 1С с сайта
	// end s-tasks